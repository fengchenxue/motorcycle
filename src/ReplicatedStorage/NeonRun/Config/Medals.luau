-- NeonRun / Config / Medals —— 奖牌阈值(秒,M8 启用)
-- Track1(M9b 首图)= P4f 标定跑基准(2026-07-16,Edit 定步长 60Hz 仿真;跳渡瞬移已按当时速度折算回墙段时间=诚实圈速口径):
--   物理上限 92.4s=全程冲刺(需 87s 冲刺≈1914 能量,经济上不可能,仅作锚点);
--   底速零失误 120.6s=纯巡航+跳跃窗冲刺(仅 9s 冲刺;44/50 缺口弹道需 133+ 速度,跳前必点火)。
-- 换算:每冲刺 1s 较巡航省 0.35s;供给上限≈34~45s 冲刺/圈(水晶 22×18+核×25+斩杀×30+闸 15+开局 50)。
-- 四档语义(阈值待人类试玩校准,标定归作者跑):
--   铜 135=完赛容错(底速+约 4 次坠落);银 120=骑顺(零失误,只在跳跃前冲刺);
--   金 112=会用经济(~30s 冲刺=吃下大半水晶);作者 105=榨干供给+心流+零重置(距物理上限留 12.6s 经济差)。
return {
	Tracks = {
		Track1 = { Bronze = 135, Silver = 120, Gold = 112, Author = 105, AuthorZeroReset = true },
	},
}
