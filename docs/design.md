# NEON RUN 设计与执行宪法 —— design.md
**版本:** v0.6(2026-07-13)| **类型:** 赛博朋克高速摩托技巧竞速 | **平台:** Roblox(移动端为主 + PC)
**结构:** 第一部分=设计篇(§0~§14);第二部分=执行篇(§E1~§E6,原独立工作计划已并入)。与旧版冲突一律以本版为准;修订台账见 §14。

---

# 第一部分 · 设计篇

## 0. 一句话定位

> **"人人 30 秒就能骑得很帅,但只有高手能骑进排行榜。"**

灵感源于《幽灵行者2》摩托关,原创赛博朋克 IP(不使用其名称/角色/美术)。差异化:Roblox 竞速被"汽车收集+拟真驾驶"占据,技巧流高速摩托跑酷是空白赛道。

## 1. 市场依据(摘要)

约 73% 已验证用户未满 18 岁 → 默认体验无惩罚性死亡;约 80% 会话在移动端 → 输入预算极小,移动端为第一公民;18+ 为增长最快群体(+50% YoY,付费高约 40%,偏好射击/RPG/竞速)→ 技巧深度与竞技排行留住高价值用户;平台规律:**惩罚"高地板",重奖"高天花板"**(Blade Ball/Rivals/Brace 全是低门槛高上限);外观为变现主体,竞技不卖数值(Brace 先例)。**战略:入口泛化、深度分层。**

## 2. 设计支柱(所有决策的裁判)

1. **速度是唯一资源,危险是唯一货币。** 所有机制汇入冲刺能量一条经济链。
2. **低地板,高天花板。** 每个系统都有自动挡兜底和手动挡上限;辅助保安全但必然更慢。
3. **一套内容,三种消费。** 三层玩家骑同一批赛道、同一套手感,差别只在目标框架与失败代价。
4. **计时公平神圣不可侵犯。** 敌人机关固定刷、不卖数值、服务端校验、跨平台输入等价、注入器零容忍。

## 3. 玩家金字塔

### 3.1 底层 —— 休闲兜风/社交(约 70%,主要付费盘)
帅+陪伴。30 秒内爽感堆料;无惩罚体验;摩托收集+全件改装+骑手装扮(步行大厅为展示场);展车/拍照/点赞;每日轻钩子。铁律:进度货币可由休闲玩法获得,永不强制进硬内容。

### 3.2 中层 —— 奖牌收集党(留存脊梁)
四级奖牌:铜(完赛)/银/金/作者牌(可要求零重置)。结算页命门:**"距下一枚奖牌差 X 秒"+一键重开(≤1s)**。幽灵系统(PB/好友/奖牌线)。**分段门实时差值(v0.5 新增):** 赛道内 3~5 个可见霓虹拱门,过门瞬间弹 `-0.42`(绿,领先)/`+0.87`(红,落后),0.8s 淡出,门框同色闪——**参照物永远是"你正在追的那个具体幽灵"**(默认自己 PB;无 PB 则追下一奖牌线幽灵;可切好友/榜首),HUD 角落常驻 `追赶:银牌线`。奖牌阶层叙事只出现在结算页(分段差值明细表=丢时间处一目了然)。无参照的首次自由跑:门只闪色不弹数字(不制造焦虑)。核心能力前 2 小时全解锁(保护榜单)。每周变体挑战。

### 3.3 顶层 —— 速通/排行(1~3%,内容与传播引擎)
留白进阶技术(点放转向/心流管理/极限走线)→ 攻略视频=免费买量。可信排行榜:赛季+名人堂+榜首回放;反作弊最高优先级。硬核模式=身份标识(一击死,专属外观仅实力可得且大厅可见)。纪录刻名/大屏轮播/月赛。UGC 编辑器为后期王牌。

### 3.4 输送管道
底→中:结算页"差 X 秒"+奖牌掉货币。中→顶:金牌瞬间"超越全球 X%"+榜首幽灵预览。顶→底:硬核外观可见、纪录刻名、最佳跑法轮播。

### 3.5 统一经济
一切活动产出同一货币,流向同一外观商店。

### 3.6 分层实现方式:规则集 × UI × 空间,**不拆地图**
规则集分层(同图:自由跑/奖牌计时/榜单+硬核开关);UI 分层(休闲默认不显计时压力,拿铜牌后渐进展示);空间内分层(主路线宽容,捷径/高架为中顶层风险回报)。唯一专层内容:教程赛道(§8)。否决存档:拆三套图=成本×3 且输送管道断裂。

## 4. 操作与核心系统

### 4.1 移动架构:自由移动 + 隐形样条
样条为数据层,六职责:前方定义、相机参考、导航、重生位姿、检查点门、辅助力场。演出段可局部上轨。

### 4.2 键位总表(每个输入只回答一个问题)

| 输入 | 回答的问题 | 说明 |
|---|---|---|
| 自动前进(赛道) | —— | 无油门,速度节奏权在设计师 |
| **A / D** | 我在哪条线上 | **二元转向,全平台统一,不做摇杆**(输入等价=跨平台榜单公平);0.12s 渐变;点放 feathering=硬核伪模拟技巧 |
| **W / Shift** | 花不花速度资源 | 冲刺,两键等价(W=平台肌肉记忆兜底:按 W=真的更快=直觉即教学);冲刺空中冻结不耗能,落地按住无缝续冲 |
| **S** | 用不用时间换容错 | 轻刹至 70%(实验开关),0.15s 渐入松开速回;与冲刺同按**刹车优先**且冲刺暂停不耗能(v0.6 拍板保留,V1 试玩 AB 终审,ADR-25) |
| **攻击键**(点按) | 时机准不准 | **斩击**(出剑):斩敌/劈回子弹/破墙/碎能量核,单键自动锁定;空挥硬直 0.25~0.35s;**任何攻击永不减速**;输入缓冲 0.1s |
| **R** | —— | 任意时刻重开(回发车线,计入重置次数,场上目标全重置) |

**空中主动姿态输入已废(v0.6/ADR-24):** 空中轨迹=起飞质量的结算(ADR-5 推论),技巧重心压在起飞前的速度管理与走线,"起跳后不可挽回"正是起飞决策的重量;完美落地(V2)独立存活;若 V3 跳跃赛道原型滞空太死,再议"俯冲单键"最小复活案。

### 4.3 移动端布局
左拇指=◀▶两个大转向键(按住);右拇指=冲刺(**点按切换**,PC 按住 Shift,设置可互换)+斩击大键(点按)+轻刹小键(按住)。"斩击点只在直线段"保证右拇指永不双键同按。(右侧竖滑已随空中输入废除,ADR-24)

### 4.4 碰撞与快速重生(v0.6 定稿,ADR-27/28)

**物性声明制(铁律):可骑面是声明的,不是涌现的。** Terrain+带 `Rideable` Tag 的面(路面/坡道/可贴墙面/演出段,TrackBuilder 自动打)=地面白名单,探针只认白名单;**其余一切 BasePart=障碍**,一律走碰撞三档。可骑坡 ≤50°/台阶 1.5 只作用于可骑面内部,不再给任意几何分类(否决存档:涌现分类下同一块石头,矮缓面被当地形→骑上→弹道脱离"弹飞",陡面被当墙→减速,同物不同果=榜单毒药)。**碰撞永不产生垂直速度**——起飞唯一来源=可骑面上的弹道脱离(ADR-5);"弹飞"作为结果类别不存在。

**碰撞三档(判定规格):** 命中瞬间按入射角(+相对速度)一次性定档,不逐帧重判。擦碰(入射角小)=火花+轻减速+镜头震,**纯惩罚不回能**(v0.6:接触回能已废,墙的能量收入专属贴墙骑行,ADR-23);滑墙=速度投影墙切线、位置永远贴墙外侧,永不向墙内推。硬撞/坠落=0.3~0.5s 故障艺术转场,**转场期间物理冻结积分**。防穿=中心+左右前角三线扫掠取最早命中(侧移也扫)。硬核模式:硬撞=死,回分段门。作者牌可要求零重置。

**快速重生=烘焙锚点链:** 烘焙期(TrackBuilder/Edit 脚本)沿样条每 `Respawn_AnchorSpacingSec`(默认 5,AB 5↔15)秒车程采样,三道 lint(向下射线命中 Rideable/直线段/前方 N studs 无危险物)通过才成为锚点(不可见 Attachment、带弧长、Studio 可手调,10~20 个/条);跳跃段上空无锚点=坠落自动回起跳坡前。运行时:普通模式=回"弧长 ≤ 当前"的最近锚点(切线朝前、贴地、恢复约 60% 速度、0.5s 输入保护,无死亡界面);硬核=回分段门。按弧长回取,不做空间搜索(上下层重叠赛道免疫);**重生弧长永不前进**。**分段门(3~5,可见,计时校验+差值)≠ 重生锚点(不可见,只管重生)**,两套点两个名字,严禁语义纠缠。语义分工:休闲=锚点续骑 / 奖牌党=R 一键重开 / 硬核=分段门重考 / 分段练习(V3)=复用分段门。

### 4.5 冲刺能量经济(v0.5 定稿)

**收入四象限(v0.6 修订版,改动走 ADR-23):**

| | 被动获取 | 主动执行 |
|---|---|---|
| **保底(蓝区)** | **充能环**:横跨路面的拱门,物理上不可能错过——向前骑即有保底收入(本质=前进门控的回能,站着一分没有) | **能量水晶** +18:贴主行车线,**磁吸半径 7 studs** 自动飞向玩家;骑过即收,不占斩击键 |
| **技巧(可入红区)** | 贴墙 +12每秒(**唯一非战斗红区收入=心流的和平路线**;接触擦墙回能已废,ADR-23)/(二期)完美落地 /(V2 候选)近失回能(贴近不触) | **能量核 +25**:悬浮发光多面体(比水晶大、带"需击破"外壳视觉),**斩击判定、无碰撞体**——骑穿未斩=纯机会成本零惩罚;摆放偏线与危险位(贴墙段末端/捷径口旁/跳台抛物线顶点);**弹反 +25** |

- **能量条分段 UI(义务):** 0~80 蓝区(谁都能充)/ 80~100 红区(仅技巧收入)。水晶受 **CrystalCap=80** 约束(理由:守住"心流=胆量勋章",防"捡垃圾进心流"与水晶路线 meta 污染榜单);蓝区充满后再吃水晶给"蓝区已满"轻提示音——**系统可以拒绝,不能沉默**。
- **冲刺规则:** 开局自带 50;耗 22/s;**最小点火时长 0.4s**(任何一次激活至少燃烧 0.4s,不足烧完为止——防碎点冲刺毁视听,封 60Hz 连点邪道);能量=0 按键给**空箱反馈**(干点火咔哒+能量条闪红)。**启动阈值废除**(按了没反应=违反低地板铁律)。
- **心流:** 满槽 100 持续冲刺进入(色调+音乐变奏+小幅加成,碰撞即断);最后 20 点必须靠技巧收入。
- **禁令与阀门:** 静止/无行为回能**永久禁止**;`Energy_MoveRegenPerSec`(骑行涓流)为**默认关闭的 AB 阀**,仅当埋点"底层玩家能量归零时长占比"证明饥荒时启用。

### 4.6 攻击系统:斩击,一刀多结算(v0.5 定稿)

**正名:** 主动词是**出剑**,"格挡"语义废弃——你攻击子弹(把它劈回去),而不是防御子弹。同一刀在不同语境结算不同结果,叙事与手感全程是进攻。

**判定实现规格(铁律):** 所有可斩目标用 CollectionService Tag 注册为脚本对象;按键帧对注册目标做**车体局部空间体积查询**(前 12 / 左右 ±7 / 上下 ±5 / 后 -2,纯数学距离判断)——**禁用物理碰撞检测**(锚定件 Touched 不触发,且物理接触受帧率影响破坏确定性)。剑光=纯 VFX,与判定零耦合;子弹=固定弹道脚本对象,弹反=改写其目标为射手。白送性质:窗口时长=体积深度÷车速,越快窗口越紧,技巧要求随速度自动升级。单次斩击结算体积内**全部**有效目标(多杀=关卡师排布的高光)。服务端按目标位置+复现位置做距离合理性校验(M12)。

**反馈铁律(v0.6/ADR-20):反馈永不冻结画面与输入。** gameplay 内禁止子弹时间/hitstop/任何时间膨胀——135 速下 0.1s 冻结=偷走 ~13 studs 读路窗口,且打断转向渐变与下一斩击窗口的预判;局部时间膨胀污染计时神圣与幽灵/共存同步。弹反爽感=非时间性反馈:音效尖峰+闪光/色差脉冲+能量条涌动+子弹肉眼可见飞回杀射手的演出。慢镜头只允许出现在结算页与回放。与"任何攻击永不减速"同源。

**快照弹道(v0.6/ADR-21):** 子弹=发射瞬间固定弹道快照,永不追踪——变速(冲刺/轻刹)与变线本身就是闪避,"加速=闪避"与支柱 1 自洽。

**敌人物性(v0.6/ADR-22):** 远程敌本体=无碰撞脚本对象,骑穿零惩罚,威胁全部来自子弹;可摆路面正中(迎面弹=最可读的"物体飞向我"弹道,强化弹反)。**生死线:敌人身体永不做碰撞障碍**——否则即 ADR-18 障碍型近战敌借尸还魂。

**交互矩阵:**

| 目标 | 窗口 | 结算 | 能量 |
|---|---|---|---|
| 远程敌·子弹 | 时间(到达瞬间,预告线) | 劈回子弹→击杀射手 | +25(破 Cap) |
| 远程敌·本体 | 空间(骑到近前) | 直接斩杀(凡敌皆可斩);本体无碰撞,骑穿=穿透零惩罚,威胁全在子弹(ADR-22) | +30(破 Cap) |
| 能量核 | 空间 | 击碎收能 | +25(破 Cap) |
| 能量闸门 | 空间 | 劈开通行(不劈=按擦碰减速) | +15 |
| 捷径挡板 | 空间 | 劈开进捷径 | 0(捷径即奖励) |
| 水晶 | —— | 骑过自动收(斩中也算收) | +18(蓝区) |
| 墙 | **不判定** | 剑光扫过冒火花(纯装饰);墙的能量归贴墙骑行专营(接触擦碰不回能,ADR-23),防"斩墙农场" | 0 |
| 幽灵/共存玩家 | 不判定 | 穿透 | 0 |

子弹命中(未弹反)=减速一档,不死。失误的代价只在"错过能量",永不在车速(空挥硬直除外)。

**废案存档(勿复活):** 枪械(瞄准=第二方向输入,移动端地板塌;抢读路注意力;弹反尖峰爽感优于射击摊平,且"劈回子弹杀射手"已含远程幻想);**障碍型近战敌**(负反馈链:0.3s 硬直@135 速=40 studs 输入锁死区,"提前按→空挥→硬直中撞敌→减速"因果稀烂;且"冲向静止物"是最难读的窗口,远劣于"物体飞向我"的弹反)——其机制位由能量核取代,其"斩敌"幻想由远程敌本体可斩承担;击飞物理与敌人主动攻击玩家永久排除(敌人是赛道元素,不是 AI);按住格挡(占用持续状态、奖励消极提前按、时机技巧蒸发);靠近自动破墙(按键时机就是玩法本体)。**V2 可选:** 判定窗口内"完美"内圈(+50% 能量+专属音画,Blade Ball 式天花板)。

### 4.7 可破坏墙家族(v0.5 排序)
- **捷径挡板=承重墙,不可砍:** 让捷径成为"带技巧税的选择"的那道税;也是节点墙的地基。
- **教学闸门=斩击键的教学锚:** 教程赛道横路一面,保证 100% 玩家第一分钟学会此键。
- **横路收费站式闸门=最弱形态:** "要按键的减速带",正式赛道少用当调味。
- **V2·前置节点联动墙(GR2 电池语法的正统移植):** 斩路边发光节点→电缆光束冲向前方墙→墙开启 N 秒(墙面倒计时灯条)→闭合。买到:编织走线(节点与墙异线)、提前读路、开窗时限=速度压力;与捷径挡板叠加=风险回报的平方。**视觉铁律:节点与墙用发光电缆物理连接+同色编码**,135 速下一眼可读。`Combat_GateLinkWindowSec=2.5`。

### 4.8 转向辅助开关(底层安全网)
自动修线+提前减速;保安全但必然更慢——"关辅助"自然成为底→中的仪式。

## 5. 手感与数值基准(Config 实时可调)

横向:渐入 0.12s/回正 0.20s,转向角速度 110↔55°/s 随速度插值。速度:巡航 100,冲刺 ×1.35,心流 ×1.08;提速 0.25s/降速 0.35s;轻刹下限 70%(渐入 0.15s)。能量:上限 100/开局 50/耗 22每秒/水晶 18(Cap 80,磁吸 7)/核 25/弹反 25/斩杀 30/闸门 15/贴墙 12每秒/最小点火 0.4s(接触擦墙回能已废,ADR-23)。重生:锚点烘焙间距 5s(AB 5↔15)/额外回退 0 锚点/恢复 60% 速度。倾斜:车体 15~25°+骑手 5~10°,回正过冲;落地压悬挂、起跳微抬头。物理:重力 260/可骑坡 ≤50°/台阶 1.5/起飞灵敏度 0.25。战斗:判定盒前 12 侧 7 上下 5 后 2/缓冲 0.1s/硬直 0.3s。声音三层:引擎随速升调/冲刺点火/近距呼啸。

## 6. 相机与视角
默认第三人称+动态档位:常规(FOV72,距 16)→冲刺档(压低拉近 FOV88+速度线微震)→心流档(近肩)。一人称=设置项(PC 优先)。理由存档:平台习惯/**皮肤可见性=付费盘生死线**/移动端晕动症/擦墙间隙可读性。弹簧跟随滞后 0.1s、转向慢半拍、撞墙回退防穿。

## 7. 关卡设计语法(TrackBuilder 的 lint 清单)

- 高速段宽走廊;截面微谷形;地标+灯带导航;可贴墙面统一霓虹条。
- 捷径全部是有意设计的风险-回报缺口;好捷径入口配挡板(技巧税)。
- **斩击/弹反判定点只放直线段,永不与转向决策重叠**(同时决策数峰值锁 2;移动端右拇指永不双键)。
- **主路线永不强制斩击(v0.6/ADR-21):** 强制斩击仅允许教程教学闸门(教学锚)与捷径挡板(技巧税);路面敌与能量闸门必须可绕行或可穿透(闸门不劈=擦碰级减速,不挡死)。
- **物性 lint(v0.6/ADR-28):** 一切可骑面必须带 Rideable Tag(TrackBuilder 自动);路面与主线旁装饰默认障碍;禁止"骑不上去又不算墙"的模糊几何。
- 水晶贴主行车线(磁吸 7 兜漏接);充能环横跨路面(保底);**能量核摆偏线与危险位**;新手赛道多环、硬核赛道多核——能量供给密度=关卡难度旋钮之一。
- 弯道按"全速+好走线可过"设计,刹车永远只是容错余量不是通关前提。
- 敌人密度:普通赛道 3~5,固定摆放确定性刷新;R 重开全部重置(验收断言)。
- **分段门 3~5 个/条(60~90s 赛道):** 可见霓虹拱门,兼任服务端校验门与岔路导航;差值显示规格见 §3.2。
- (V2)节点-墙电缆连接+同色编码为视觉铁律。
- 待定:爽段/技巧段/演出段配比模板。

## 8. 大厅与进入流程
**原则:进门即玩,永不等人,没有"房间"概念,任何菜单都是流失点。**
步行大厅(Avatar 步行=平台社交母语+骑手装扮展示场;直径 ≤200 studs);门=空间化选关(锁门显示解锁条件);**进门自动上车仪式**:穿门→0.5s 上车动画→滑行至发车线→**3-2-1 固定点火自动起步**(发车标准化:服务端计时自绿灯起算,无抢跑,首个决策=第一个弯的走线);**首登直进教程赛道**(60~90s 剧本:转向→贴墙→冲刺→斩击,保送铜牌;带奖牌与货币初见大厅;设跳过口);老玩家出生大厅面朝大门广场;单人练习=每条赛道默认态(幽灵陪跑,同服半透明共存各自计时);结算页主按钮=一键重开;训练场=一扇普通门(无计时,水晶/可贴墙面/弹反木桩);竞技场门(V3)整点回合、等待区可热身;好友加入/VIP 私服走平台原生。分段练习(分段门起跑)记 V3。

## 9. 社交三层
洞察:无碰撞下"实时竞速"≈"同步播放的真人幽灵",三层共用地基。L1 异步幽灵(V1~V2,复用反作弊回放系统;变强引擎)→ L2 无碰撞共存(V2 随 M12;氛围与橱窗,半透明各自计时)→ L3 定时回合赛+月杯赛(V3,赛事包装)。**永久排除带碰撞对抗**。UI 走聪明默认值,不做幽灵管理界面;二选一留幽灵。

## 10. 商业化与公平
卖:车外观/尾焰/骑手装扮/赛季通行证/新手包/VIP 双倍/VIP 私服;可选付费 EA。不卖任何影响计时的数值;核心能力 2 小时全解锁。反作弊:服务端速度/位置/门序校验、前十回放人工复核、注入器永久封禁。合规:血腥收敛(敌人=机器溶解成数据碎光)、原创 IP。

## 11. 检查点四功能拆解(v0.6 修订)
服务端校验门(防抄近道/计时)=必须,已建(M8)。重生=烘焙锚点链(§4.4/ADR-27):普通回最近锚点、硬核回分段门;**锚点与门是两套点,严格分开命名**。**可见分段门+差值显示**=新增(§3.2/§7)。分段练习起点=V3,复用同批门定义。

## 12. 分层健康指标
底层:D1/会话频次/ARPDAU/**能量归零时长占比**。中层:D7/D30/人均奖牌/结算页重开率。顶层:人均时长/榜单参与率/视频产出。

## 13. 未决事项
爽段/技巧段/演出段配比模板;经济数值 V1 试玩标定(轻刹 / MoveRegen / **重生锚点间距 5↔15** 的 AB);完美判定(V2)数值;节点墙(V2)细节;近失回能(V2 候选)判定细节;反重力上轨演出段(V3)相机滚转规格;斩击/引擎正式音频素材(人工挑选 assetId);正式名称/美术方向/教程赛道具体剧本(人类主导)。

## 14. 修订台账
**v0.5→v0.6(ADR-20~28):** 反馈铁律入宪(gameplay 禁子弹时间/hitstop,反馈永不冻结画面与输入);快照弹道=变速即闪避;lint 新增"主路线永不强制斩击";路面远程敌定稿(本体无碰撞,威胁全在子弹);接触擦墙回能删除(贴墙 +12/s 保留为唯一非战斗红区收入,近失回能记 V2 候选);空中 W/S 主动输入删除(被动规则与完美落地保留);S 轻刹拍板保留(V1 AB 终审);天花板/反重力=上轨演出段立项 V3(自由移动反重力否决);重生定稿=烘焙锚点链(普通回锚点/硬核回分段门;否决稀疏门重生、运行时裸算最近点、手摆密集锚点);物性声明制(Rideable 白名单)+碰撞零垂直速度+判定五规格(M4.1 立项);节点联动墙确认维持 V2 不提前。
**v0.4→v0.5:** 能量启动阈值废除→最小点火 0.4s+空箱反馈;收入四象限封版(磁吸 7/充能环分工/能量核 +25 破 Cap/MoveRegen AB 阀/能量条 80 分段 UI);斩击正名与判定规格(体积查询+Tag,禁物理碰撞;硬直 0.3s;攻击永不减速;缓冲 0.1s);交互矩阵入册(斩墙零收益);障碍型近战敌废弃(全息哨兵一并撤销,能量核取代机制位);墙家族排序+V2 节点联动墙;检查点四功能拆解+分段门差值显示(追单一具体幽灵);TrackBuilder 立项(M8.5)+首图混合分工;近战敌规格类(击飞/敌人 AI)永久排除。**v0.3→v0.4:** 见 git 历史中 v0.4 文档 §14。

---

# 第二部分 · 执行篇(原工作计划并入)

## E1. 守则(浓缩)
本文档设计篇是宪法;全参数进 Config;灰盒优先;手感问题停下问人类;每里程碑独立可玩+自动验收;破坏性操作先备份;不确定就问。交付模板:变更摘要→验收结果→试玩指引→已知问题→下一步。会话收尾:更新 status.md(快照+ADR)。

## E2. 技术基线
Luau + Rojo;禁 VehicleSeat/约束驱动;玩家摩托客户端权威模拟+服务端校验,计时奖牌只在服务端产生;防穿=扫掠射线(长度=|v|·dt+余量);样条 Catmull-Rom+弧长表+最近点查询;战斗判定=局部体积查询+Tag(禁 Touched);物理/视觉分离(simPos);性能:PC 中端 60fps、中端手机 30fps;敌人机关全固定确定性。

## E3. 里程碑总表

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 目录/配置/ConfigLive 调参 | | ✅ |
| M1 样条数据层 | | ✅ |
| M2 摩托核心控制器(Spike:A 胜出) | | ✅ |
| M2.1 键位补丁(S 轻刹+W 别名) | | 待核实(见 status) |
| M3 相机与表现层(人工验收点) | | ✅(按用户报告,细节待盘点) |
| M4 碰撞三档与重生 | | ✅(同上) |
| M5 能量/冲刺/心流 | | ✅(同上;v0.5 增量见 E4) |
| M6 贴墙骑行 | | ✅(同上) |
| M7 斩击/弹反 | | ✅(同上;v0.5 增量见 E4) |
| M8 计时/检查点门/奖牌/结算 | | 🔶 逻辑✅但**仅客户端灰盒**(RaceTimer);服务端权威=欠账归 M12(ADR-32) |
| **M8.1 v0.5/v0.6 增量落地** | 分段门差值/能量核/最小点火/磁吸/分段 UI/交互矩阵对齐/接触擦墙回能移除 | ⬅️ 下一步 |
| **M4.1 重生与碰撞确定性** | Rideable 物性白名单/碰撞五规格+零垂直速度/烘焙锚点链重生/石头与角度×速度矩阵验收 | 排队(M8.1 后) |
| **M8.5 TrackBuilder** | 样条→路面生成+语法摆放+lint(含 Rideable 自动打 Tag+锚点烘焙) | 排队 |
| M9a 教程赛道 / M9b 首条正式赛道 | 混合分工流程 | 排队 |
| M10 移动端输入与性能 | 布局按 §4.3 | 排队 |
| M11 步行大厅+门系统+训练场门+首登路由 | | 排队 |
| M12 幽灵/排行榜/服务端反作弊/L2 共存 | | 排队 |
| V3 backlog | 跳跃赛道/反重力上轨演出段/硬核模式/竞技场定时赛/分段练习/节点墙/完美判定/近失回能/周变体/VIP 私服 | 排队 |

## E4. 近期任务详单

**M8.1 v0.5 增量落地(先做,对齐宪法):**
1. Config 新增/修订:`Energy_MinIgnitionBurnSec=.4`、`Energy_IgnitionCost=0(AB)`、`Energy_CrystalMagnetRadius=7`、`Energy_MoveRegenPerSec=0(AB,默认关)`、`Energy_CoreGain=25`、`Energy_GateGain=15`、`Combat_HitboxForward/Side/Up/Back=12/7/5/2`、`Combat_InputBufferSec=.1`、`Combat_RecoverySec=.3`、`Combat_GateLinkWindowSec=2.5(V2 预埋)`;**删除/置零能量启动阈值字段**;**删除/置零接触擦墙回能字段(实际键名待 M8 盘点核实;贴墙每秒回能保留,ADR-23)**。
2. 能量系统:最小点火逻辑+空箱反馈;水晶磁吸;能量条 80 分段渲染+"蓝区已满"提示;能量核实体(Tag+斩击判定+无碰撞,破 Cap 入账);移除接触擦墙回能路径(擦碰事件只惩罚不入账,ADR-23)。
3. 斩击系统对齐交互矩阵:确认判定为体积查询非 Touched;空挥硬直与"攻击永不减速"断言;斩墙零收益;单次多目标结算;R 重开目标全重置断言。
4. 分段门:把 M8 校验门中 3~5 个渲染为可见拱门,过门弹 ± 差值(追踪目标幽灵接口先以 PB/奖牌线时间表实现,幽灵实体 M12 补);无参照只闪色。
5. 验收:Edit 仿真——点火不足 0.4s 自动烧满;0 能量按键触发空箱反馈且速度不变;磁吸半径边界拾取;核/门/敌各结算路径能量入账正确;复跑直线 0 误差+坡顶起飞回归。

**M4.1 重生与碰撞确定性(M8.1 后,ADR-27/28):**
1. 物性:`Rideable` Tag 白名单(Terrain 默认可骑;**既有测试 rig 地面件补 Tag,否则起飞回归假失败**);探针只认白名单;selfcheck 加白名单核对项。
2. 碰撞:定档一次性(命中瞬间按入射角定档,不逐帧重判)/擦碰滑墙永不向墙内推/硬撞转场冻结积分/中心+左右前角三线扫掠/**任意碰撞 vy 增量=0**。
3. 重生:过渡版 Edit 烘焙脚本(样条采样+三道 lint→锚点 Attachment,TrackBuilder 就绪后由其接管);按弧长回取;Config 新增 `Respawn_AnchorSpacingSec=5`、`Respawn_SetbackAnchors=0`;硬核=分段门重生(接口预留)。
4. 验收:石头 rig 同线 100 次结果全同;入射角(5/15/30/45/60/90°)×速度(100/135/146)矩阵档位正确+零穿透+零墙内帧;锚点枚举全过三 lint;重生弧长永不前进;复跑直线 0 误差+坡顶起飞回归。

**M8.5 TrackBuilder(工具,半个~一个里程碑):**
输入样条 → 生成路面几何(宽走廊/微谷截面)+检查点门+按 §7 语法自动摆放(水晶主线/核偏线/环保底/斩击点限直线段)→ **lint 报告**(弯道半径 vs 全速转向半径、判定点入弯检测、辅助开启下机器人完赛测试、能量供给密度统计)。改一版=分钟级。

**M9 首图(混合分工接口):** 人类=导演(一句话主题、拖 CP 橙球画宏观形、试驾口头反馈、终审;可让 AI 先出 3 版布点供挑);AI=施工队(TrackBuilder 生成+自检报告+按反馈迭代)。**M9a 教程赛道例外:剧本节拍(转向→贴墙→冲刺→斩击,保送铜牌)人类主导,AI 纯施工。**

## E5. 每里程碑验收方式
运动学核心可在 Edit 模式固定 dt=1/60 同步仿真(不进 Play);基准与回归对照表见 `docs/status.md`;改贴地/起飞必复跑"坡顶起飞回归";人工验收点(手感类)必停,附试玩指引与问题清单。

## E6. 禁止事项
见根目录 CLAUDE.md「废案黑名单」与「技术铁律」,此处不重复(单一事实源)。
